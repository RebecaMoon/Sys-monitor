services:
  database:
    image: postgres:15-alpine
    container_name: sys_monitor_db

    environment:
      POSTGRES_USER: reb_admin
      POSTGRES_PASSWORD: Z4mperf1@
      POSTGRES_DB: monitor_db

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: